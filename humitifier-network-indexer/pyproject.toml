[project]
name = "humitifier-network-indexer"
version = "5.0.0"
description = ""
authors = [
    {name = "Mees, T.D. (Ty)",email = "t.d.mees@uu.nl"}
]
license = {text = "GPL-3"}
readme = "README.md"


[tool.poetry.dependencies]
python = "^3.14,<3.15"
toml = "^0.10.2"
pydantic-settings = "^2.14.2"
humitifier-common = { path = "../humitifier-common", develop = true }
sentry-sdk = {extras = ["celery"], version = "^2.22.0"}
dnspython = "^2.8.0"
celery = {extras = ["redis"], version = "5.6.3"}

[tool.poetry]

[tool.poetry.group.dev.dependencies]
black = "^26.5.1"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
